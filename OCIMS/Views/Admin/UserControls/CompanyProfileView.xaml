<UserControl x:Class="eSureHi.Views.Admin.UserControls.CompanyProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:helpers="clr-namespace:eSureHi.Helpers"
             xmlns:local="clr-namespace:eSureHi.ViewModels.Admin"
             d:DataContext="{d:DesignInstance local:CompanyProfileViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Margin="0,0,0,24" BorderBrush="#DDE4EF" BorderThickness="0,0,0,1" Padding="0,0,0,12">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Domain" Width="32" Height="32" Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <TextBlock Text="Company Profile" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>
                <Button Command="{Binding BackToDashboardCommand}"
                        Background="#D32F2F"
                        Foreground="White"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Height="34"
                        Padding="16,0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Close" Width="16" Height="16" Margin="0,0,8,0"/>
                        <TextBlock Text="Exit" FontSize="12" FontWeight="Medium"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Form Content -->
        <materialDesign:Card Grid.Row="1" Padding="32" MaxWidth="600" HorizontalAlignment="Left" VerticalAlignment="Top" Background="White" helpers:Adaptive.UseAdaptiveForeground="True">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Name -->
                    <TextBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding Profile.Name}" materialDesign:HintAssist.Hint="Company/Organization Name" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,16" FontSize="14"/>

                    <!-- Address -->
                    <TextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding Profile.Address}" materialDesign:HintAssist.Hint="Address" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,16" FontSize="14" AcceptsReturn="True" MaxHeight="100" TextWrapping="Wrap"/>

                    <!-- Contact Number -->
                    <TextBox Grid.Row="2" Grid.Column="0" Text="{Binding Profile.ContactNumber}" materialDesign:HintAssist.Hint="Contact Number" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,16,16" FontSize="14"/>

                    <!-- Email -->
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Profile.Email}" materialDesign:HintAssist.Hint="Email Address" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,16" FontSize="14"/>
                    
                    <Grid Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding Profile.LogoPath}" materialDesign:HintAssist.Hint="Logo Path" Style="{StaticResource MaterialDesignFloatingHintTextBox}" FontSize="14"/>
                        <Button Grid.Column="2" Command="{Binding BrowseLogoCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Height="36" Padding="10,0" ToolTip="Select logo image">
                            <materialDesign:PackIcon Kind="FolderImage" Width="16" Height="16"/>
                        </Button>
                        <Button Grid.Column="4" Command="{Binding ClearLogoCommand}" Style="{StaticResource MaterialDesignIconButton}" Height="36" Width="36" ToolTip="Clear logo path">
                            <materialDesign:PackIcon Kind="Close" Width="16" Height="16"/>
                        </Button>
                    </Grid>
                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <ProgressBar IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}" Width="30" Height="30" Margin="0,0,16,0" Foreground="{StaticResource PrimaryBrush}"/>
                    <Button Content="SAVE PROFILE" Command="{Binding SaveCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Width="150" Height="40" Background="{StaticResource PrimaryBrush}" Foreground="White"/>
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
